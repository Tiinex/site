# Continuity Context

- Envelope Schema: [tiinex.root.v1](https://github.com/Tiinex/docs/blob/3988951208eb9a8926e84ab42625d4b42fa00c2d/.topics/.schemas/tiinex.root.v1.schema.md)
- Current
  - Current Schema: [tiinex.decision.v1](https://github.com/Tiinex/docs/blob/e713557f8be630967571d11a73f9ecd05ae329ce/.topics/.schemas/core/decision/tiinex.decision.v1.schema.md)
  - Created At: 2026-08-24 23:11:00
  - Authors: Loom
  - Why: Close the independently reproduced full-source material-closure regression without weakening the accepted exact selected-Handoff conformance gate, and return the correction through the existing single-primary human carrier contract.
  - Summary: Tooling 027-3-2 correction — schema-valid carried Handoff test fixtures restore the full-source material-closure regression while preserving the fail-closed 027-3 readiness boundary.
  - Status: complete/local

---

# Tooling 027-3-2 result — full-source material-closure regression correction

The stale material-closure Handoff fixtures are migrated behind the accepted exact selected-Handoff conformance gate; the bounded portable regression set is green in the supplied correction runtime, with no production qualification logic weakened or changed.

## Decision

- State: correction-complete / independent-acceptance-pending
- Subject: full-source `materialClosure.test.mjs` regression exposed by Tooling 027-3 selected-Handoff conformance
- Decision: retain the Tooling 027-3 fail-closed implementation unchanged and qualify the material-closure test harness itself. The selected test Handoff is now c14n-v2 sealed through `qualifiedHandoffFixture`, carried as a real dimensioned workspace route byte, and correlated through the same route/materialization path production manufacture requires.
- Boundary: this result changes regression fixtures/test harness only; it does not alter `selectedHandoffConformance`, carrier representation, canonical schemas, c14n-v2 algorithms, production material-closure behavior, workspace/archive classification, or remote state.

## Basis

- The independently reported failure reproduced exactly: the anonymous-complete package case returned `blocked` where the pre-027-3 test expected `ready` because its old structurally minimal Handoff fixture no longer satisfies the accepted selected-route contract.
- Replacing only the Markdown body was insufficient, correctly: the 027-3 carrier projection also requires the selected Handoff to be a real carried workspace byte at a dimensioned `.trace.md` route. The test harness now injects that exact sealed byte into its workspace materializations and supplies the matching transport route.
- Handoff A and Handoff B are generated and sealed independently. The prior test mutated Handoff A text to create B, which is incompatible with self-integrity qualification because edited bytes must not inherit a stale seal.
- The former reordered-correlation ready case used duplicate workspace ids (`docs`, `docs`). Under the accepted route semantics that is independently ambiguous. That ready-only fixture now uses `docs-a` and `docs-b`; the dedicated duplicate-id and true-collision cases retain duplicate ids and still assert blocking/ambiguity behavior.
- The duplicate-workspace package-file count assertion excludes the additional selected-Handoff route carrier so it continues to count the two material-under-test carriers rather than conflating route grounding with duplicate workspace payload evidence.

## Regression Evidence

- `src/tooling/portable/handoff/materialClosure.test.mjs`: pass after fixture migration.
- Focused portable Handoff regressions replayed green in the composed supplied runtime: `handoff.manufacture.test.mjs`, `materialClosure.test.mjs`, `carrierProjection.test.mjs`, `pointerEntrypoint.test.mjs`, `coldConsumerEntrypoint.test.mjs`, `coldStartQualification.test.mjs`, `contextAudit.test.mjs`, `multiRootManufacture.test.mjs`, `handoff.manufacture.scale.test.mjs`, `humanOutputCopyablePresentation.test.mjs`, `humanOutputNormalEmission.test.mjs`, and `transportCompanion.test.mjs`.
- Scale replay remained green at `1,286` workspace carriers and `1,306` package files in about `6.2 s` on this local observation. Timing is evidence only, not an SLA.
- TypeScript check `npx --no-install tsc -p tsconfig.json` passed.
- The correction carrier does not contain the separate `routeArtifactConformance.test.mjs` file, so this Loom replay does not claim a fresh execution of that test. Anchor's prior full-source independent review recorded it green.
- Architecture shape, browser import boundary, and schema-binding commands cannot be meaningfully replayed from this route-scoped carrier because unrelated full-app/schema source files are intentionally omitted (`src/schemas/workspace/workspace.views.jsx`, `src/main.jsx`, and schema manifest/snapshot material). Anchor's prior full working-source replay recorded those checks green after the 027-3 overlay. This correction changes only `materialClosure.test.mjs`, not production/static source.

## Consequences

- The full-source regression blocker is closed locally without a conformance bypass or production implementation change.
- Exact selected-Handoff Root/schema/self/Parent-target fail-closed qualification remains controlling.
- Complete/partial qualification, caller-readiness bypass resistance, anonymous workspace projection, duplicate ids, provider ambiguity, required/reference closure, exact package bytes, stale-plan mismatch behavior, and the other pre-existing material-closure scenarios remain exercised behind a valid Handoff contract.
- Anchor retains independent acceptance authority. Axiom workspace/archive binding classification remains gated until this correction is accepted.
- The return uses the current carrier format only; it does not trigger the future first-new-format Sigma inspection gate.
- Normal human presentation must expose exactly one package choice plus the exact generated inline routing. Result/evidence artifacts remain carried material, not parallel transport choices.

## Limits

- No carrier migration, workspace/archive semantic classification, canonical schema edit, integrity-algorithm edit, source-byte normalization, publication, authentication, commit, push, credential flow, or other remote mutation was performed or authorized.
- Two harness-only shims were necessary to execute the route-scoped correction package in isolation: a minimal `tree.bundle.js` dependency stub and a synthetic historical dogfood Handoff equivalent. They were used only to reproduce/evaluate the supplied slice and are not included in this returned workspace. The canonical historical Handoff was independently confirmed from the referenced repository revision.
- Because the supplied carrier is route-scoped rather than the complete repository, this result distinguishes checks actually rerun here from prior full-source evidence and does not promote packaging omissions into implementation failures.

# Continuity Integrity

- [sha256-base64url-c14n-v2](https://github.com/Tiinex/docs/blob/3988951208eb9a8926e84ab42625d4b42fa00c2d/.topics/.validators/sha256-base64url-c14n-v2.validator.md)
  - Towards: self
  - Value:6LQvi8gfFJ3RocVcS1Pch_-4IbS7e_Z0E-Jt9-TuDAo
