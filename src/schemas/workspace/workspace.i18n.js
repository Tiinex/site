export const workspaceI18n = Object.freeze({
  label: 'Workspace',
  localBoundary: 'Browser-local session state; no source files or GitHub provenance inferred.',
  emptyResult: 'No artifacts yet — add local Markdown to get started. Click + (Add) or drop .md files here. Local files remain local/session only.',
  legacyEmptyResult: 'No nodes match this view.',
  dropHint: 'Add local Markdown via + (Add) or drop .md files here. For source-backed material use Add → GitHub source and provide explicit file paths.'
});
