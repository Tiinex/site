export const workspaceI18n = Object.freeze({
  label: 'Workspace',
  localBoundary: 'Browser-local session state; no source files or GitHub provenance inferred.',
  emptyResult: 'No nodes match this view.',
  dropHint: 'Drop lineage files, configs, or folders into this workspace · or use Add for source-backed loading when the adapter is enabled.'
});
