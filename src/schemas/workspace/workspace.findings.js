export const workspaceFindings = Object.freeze({
  localBoundary: { severity: 'info', code: 'workspace.local.boundary' },
  sourceAttached: { severity: 'info', code: 'workspace.source.attached' },
  progressActive: { severity: 'info', code: 'workspace.source.progress.active' }
});
