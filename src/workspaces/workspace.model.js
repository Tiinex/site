export const WorkspaceMode = Object.freeze({ local: 'file-local', sourceBacked: 'source-backed', mixed: 'mixed' });
export const WorkspaceRecordState = Object.freeze({ active: 'active', loaded: 'loaded', unavailable: 'unavailable' });
