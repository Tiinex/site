{
  "name": "tiinex-site",
  "version": "0.2.0-v96",
  "private": true,
  "description": "Fresh Tiinex site shell with focused Tiinex.dev pattern parity and UI-shape guards, Universe/Column Verse default workspace continuity, workspace-level Map Verse scaffold, context-aware Verse availability, renderer boundaries, source controls, artifact parsing, root fallback, source boundaries, audit skeleton, and legacy behavior reference.",
  "type": "module",
  "scripts": {
    "validate": "node tools/validate-static.mjs && node tools/validate-schema-bindings.mjs",
    "ui:shape": "node tools/check-ui-shape.mjs",
    "test": "npm run validate && npm run ui:shape && npm run build:public && npm run public:check",
    "metrics": "node tools/collect-metrics.mjs",
    "storage:scan": "node tools/inspect-storage.mjs",
    "build:public": "node tools/build-public.mjs",
    "public:check": "node tools/check-public-build.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
