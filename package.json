{
  "name": "tiinex-site",
  "version": "0.2.0-v92",
  "private": true,
  "description": "Fresh Tiinex site shell with Universe/Column Verse default workspace continuity, scoped implemented verses only, context-aware Verse availability, interaction spine, ergonomic source/discovery controls, artifact parsing, root fallback, source boundaries, audit skeleton, and legacy behavior reference.",
  "type": "module",
  "scripts": {
    "validate": "node tools/validate-static.mjs && node tools/validate-schema-bindings.mjs",
    "test": "npm run validate && npm run build:public && npm run public:check",
    "metrics": "node tools/collect-metrics.mjs",
    "storage:scan": "node tools/inspect-storage.mjs",
    "build:public": "node tools/build-public.mjs",
    "public:check": "node tools/check-public-build.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
