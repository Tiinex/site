{
  "name": "tiinex-site",
  "version": "0.2.0-v99",
  "private": true,
  "description": "Tiinex Site shell with Column-only runtime scope, focused Tiinex.dev pattern parity, UI-shape guards, legibility correction, source boundaries, artifact parsing, root fallback, audit skeleton, bundled public build, and legacy behavior reference.",
  "type": "module",
  "scripts": {
    "validate": "node tools/validate-static.mjs && node tools/validate-schema-bindings.mjs",
    "ui:shape": "node tools/check-ui-shape.mjs",
    "test": "npm run validate && npm run ui:shape && npm run build:public && npm run public:check",
    "metrics": "node tools/collect-metrics.mjs",
    "storage:scan": "node tools/inspect-storage.mjs",
    "build:public": "node tools/build-public.mjs",
    "public:check": "node tools/check-public-build.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
