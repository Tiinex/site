# Tiinex Site v96

Focused Tiinex.dev pattern parity plus the first machine UI-shape guard.

This source package keeps the fresh runtime ownership from the refactor, but tightens the default Column/Universe entry toward the archived Tiinex.dev pattern baseline: one centered workspace window, compact controls, Documentation/Start continuity cards, badge-title-action card rhythm, and secondary diagnostics hidden away.

The public build now emits bundled `tiinex.bundle.css` and `tiinex.bundle.js` for faster CI/CD-published loading while keeping the source tree auditable. The delivered zip is still a source-clean repo replacement package. It is not a dist/deploy artifact. CI/CD builds public output after push.
